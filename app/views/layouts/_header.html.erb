<nav class="static bg-blue-900 shadow min-w-screen" data-controller="dropdown">
  <div class="w-auto max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex-shrink-0 flex items-center">
        <div class="flex">
          <%= link_to root_path do %>
            <div class="flex items-center">
              <div class="inline-block h-8 justify-self-start content-start items-start">
                <%= image_tag 'block_m-hex.png', alt: 'University of Michigan Logo', height: '35.75', width: '50', class: '' %>
              </div>
              <span class="text-white font text-3xl pl-2 font-sans font-medium">
                Art Survey
              </span>
            </div>
          <% end %>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-4 pl-2">
          <div class="border-transparent text-blue-50 hover:border-blue-300 hover:text-white inline-flex items-center pr-1 pt-1 border-b-2 text-base font-normal"><%= link_to "Catalog", art_items_path, class: "#{class_names(active: current_page?(art_items_path))}"%></div>
          <div class="border-transparent text-blue-50 hover:border-blue-300 hover:text-white inline-flex items-center pr-1 pt-1 border-b-2 text-base font-normal"><%= link_to "AdminDoc", "https://docs.google.com/document/d/1pmgKA3ZOaOsgaPZjhe2COJIYtvJkXHF8QQBXXqpnOTg/edit?usp=sharing", target: "_blank"%></div>
          <% if user_signed_in? %>
            <%= button_to destroy_user_session_path, method: :delete, data: {turbo: "false"} do %>
              <div class="border-transparent text-blue-50 hover:border-blue-300 hover:text-white inline-flex items-center pr-1 pt-1 border-b-2 text-base font-normal">Sign Out</div>
            <% end %>
            <div class="border-transparent text-yellow-500 inline-flex items-center pr-1 pt-1 border-b-2 text-base font-normal">
              <%= current_user.email %>
            </div>
          <% else %>
            <%= button_to user_saml_omniauth_authorize_path, data: {turbo: "false"} do %>
              <div class="border-transparent text-blue-50 hover:border-blue-300 hover:text-white inline-flex items-center pr-1 pt-1 border-b-2 text-base font-normal">Sign In</div>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>